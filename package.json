{
  "name": "resume-analyzer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client --production=false && npm run build --prefix client && npm install --prefix server --production",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
